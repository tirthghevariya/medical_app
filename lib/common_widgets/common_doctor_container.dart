import 'package:flutter/material.dart';

class CommonDoctorsContainer extends StatelessWidget {
  const CommonDoctorsContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
          child: Container(
        color: Colors.red,
      )),
    );
  }
}
